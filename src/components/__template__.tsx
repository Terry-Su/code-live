import React from "react"
import mapStateStyle from "../utils/mapStateStyle";
import BasicComponent from "./BasicComponent";

export default mapStateStyle({
  
})(
  class __Template__ extends BasicComponent {
    render() {
      const { c } = this
      return <div></div>
    }
  }
)
